int fact(int n ,int i)
{
	if(n == 1)
		return n;
	else
		return (n * fact(n -1, 0));
}

int main()
{
	int k = 1; 
	int m, result;
	m = read();
	if(m > 1)
		result = fact(m, 0);
	else
		result = 1;
	write(result);
	return 0;
}